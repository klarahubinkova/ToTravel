<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AddPointActivity">

    <TextView
        android:id="@+id/addPointText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/add_point_heading"
        android:textSize="@dimen/text_size"
        android:textColor="?attr/colorPrimary"
        android:textStyle="bold"
        android:gravity="center"

        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/addByNameButton"/>


    <Button
        android:id="@+id/addByNameButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:contentDescription="@string/add_by_name_button"
        android:paddingVertical="12dp"
        android:text="@string/add_by_name"
        android:textSize="@dimen/middle_text_size"

        app:layout_constraintTop_toBottomOf="@id/addPointText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintWidth_percent="0.8"/>

    <Button
        android:id="@+id/addByCoordinatesButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:contentDescription="@string/add_by_location_button"
        android:paddingVertical="12dp"
        android:text="@string/add_by_location"
        android:textSize="@dimen/middle_text_size"

        app:layout_constraintTop_toBottomOf="@id/addByNameButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.8" />

</androidx.constraintlayout.widget.ConstraintLayout>